// Day 66 Pretest
// Rule: no Google / no AI / no old files

/*
Answer from memory before opening notes.

1. What is the difference between a .env file and an OS environment variable?

2. Why can process.env.API_KEY be undefined even when .env contains API_KEY?

3. What does export API_KEY=abc do that API_KEY=abc alone may not do?

4. Why is PATH an environment variable? What bug happens when PATH is wrong?

5. Name three places where "local env" and "production/service env" can differ.

6. If React frontend needs to use a paid third-party API, where should the secret
   API key live? Why?

7. Is NEXT_PUBLIC_API_KEY safe for a real secret? Explain.

8. What is the difference between a public/publishable key and a secret key?

9. List five controls that reduce abuse when the browser calls your backend API.

10. If an API key leaks, what are the first three actions you take?

11. Why is "send an API key from frontend to backend" usually weaker than using
    a user session, httpOnly cookie, or short-lived access token?

12. What security layers would you combine to reduce abuse as much as practical:
    auth, authorization, rate limit, quota, IP allowlist, origin check, CSRF,
    logging, key rotation?

13. What is the difference between authentication and authorization?

14. Why is "hide the admin button in frontend" not real security?

15. How do parameterized queries reduce SQL injection risk?

16. What is XSS, and why does localStorage make token theft worse?

17. When does CSRF matter? How do SameSite cookies and CSRF tokens help?

18. What should you validate before accepting a file upload?

19. Name five things that should never appear in logs.

20. What should a backend do before trusting a webhook request?

21. List the OWASP Top 10:2025 categories from memory.

22. Which OWASP 2025 category covers bad CORS, debug mode, open storage buckets,
    default credentials, or missing security headers?

23. Which OWASP 2025 category covers package/build/CI dependency risk?

24. Which OWASP API 2023 risks map to "user changes an ID and sees someone
    else's object" and "user submits fields they should not be allowed to edit"?

25. Which OWASP API 2023 risk maps to expensive provider calls without rate
    limits, quotas, size limits, or timeouts?

26. What does "fail closed" mean when authz, quota, payment, or webhook checks
    throw an exception?
*/
